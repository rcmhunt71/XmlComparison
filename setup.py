#!/usr/bin/env python

from distutils.core import setup

setup(name='URLA_XML',
      version='0.0.1',
      description='Fiserv ULAD XML Validation tool',
      author='Fiserv',
      author_email='',
      packages=[],
      requires=['xmltodict', 'PrettyTable'],
      )